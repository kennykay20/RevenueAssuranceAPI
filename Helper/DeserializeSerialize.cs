using System.Collections.Generic;
using System.Threading.Tasks;
using Newtonsoft.Json;

namespace RevAssuranceApi.Helper
{
    public class DeserializeSerialize<T>
    {

        /*public async Task<string> Serialize(T obj)
        {
             var jos = JsonConvert.SerializeObject(obj);
             return jos;
        }

          public async Task<T> DeSerialize(string stringObject)
        {
            var des = JsonConvert.DeserializeObject<T>(stringObject);
             return des;
        }
       */
    }
}